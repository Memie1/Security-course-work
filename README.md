Flask based web app


first ensure to have a python venv

first ensure cd into backend
python -m venv venv

activate
venv/Scripts/activate

pip install -r requirements.txt

python -m app

backend contains flask package and the frontend holds jinja2 templates

